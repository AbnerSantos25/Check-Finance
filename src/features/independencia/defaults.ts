import type { IndependenceParams } from '../../types';

export const DEFAULT_PARAMS: IndependenceParams = {
  monthlyIncomeGoal: 10000,
  currentWealth: 0,
  monthlyContribution: 1000,
  annualReturn: 10,
  annualInflation: 4.5,
  currentAge: 30,
  contributionFollowsInflation: true,
};
